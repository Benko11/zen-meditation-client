import React from 'react';
import styled from 'styled-components';

export const SectionTitle = styled.h2`
    border-bottom: 2px solid var(--theme-colour2);
    margin-bottom: 5px;
    padding-bottom: -5px;
`;
