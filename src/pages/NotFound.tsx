import React from 'react';
import { Title } from '../components/Title.styled';

const NotFound = () => {
    return (
        <div>
            <Title>Stay in zen</Title>
        </div>
    );
};

export default NotFound;
